interface:
  display_name: "Neo Legend Luxury Combo Chart (Neo Legend 组合图表)"
  short_description: "Tune `combo_chart` chart styling and data rules. (调优 `combo_chart` 图表的样式和数据规则。)"
default_prompt: "Use $neo-legend-combo-chart to tune the Neo Legend `combo_chart` renderer. (使用 $neo-legend-combo-chart 来调优 Neo Legend `combo_chart` 渲染器。)"
policy:
  allow_implicit_invocation: true
