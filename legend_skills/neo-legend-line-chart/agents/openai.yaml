interface:
  display_name: "Neo Legend Luxury Line Chart (Neo Legend 折线图)"
  short_description: "Tune `line_chart` chart styling and data rules. (调优 `line_chart` 图表的样式和数据规则。)"
default_prompt: "Use $neo-legend-line-chart to tune the Neo Legend `line_chart` renderer. (使用 $neo-legend-line-chart 来调优 Neo Legend `line_chart` 渲染器。)"
policy:
  allow_implicit_invocation: true
