interface:
  display_name: "Neo Legend Luxury Sankey Diagram (Neo Legend 华丽桑基图)"
  short_description: "Tune `sankey_chart` chart styling and data rules. (调优 `sankey_chart` 图表的样式和数据规则。)"
default_prompt: "Use $neo-legend-sankey-chart to tune the Neo Legend `sankey_chart` renderer. (使用 $neo-legend-sankey-chart 来调优 Neo Legend `sankey_chart` 渲染器。)"
policy:
  allow_implicit_invocation: true
