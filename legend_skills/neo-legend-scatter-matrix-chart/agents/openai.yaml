interface:
  display_name: "Neo Legend Executive Scatter Matrix (Neo Legend 散点矩阵图)"
  short_description: "Tune `scatter_matrix_chart` chart styling and data rules. (调优 `scatter_matrix_chart` 图表的样式和数据规则。)"
default_prompt: "Use $neo-legend-scatter-matrix-chart to tune the Neo Legend `scatter_matrix_chart` renderer. (使用 $neo-legend-scatter-matrix-chart 来调优 Neo Legend `scatter_matrix_chart` 渲染器。)"
policy:
  allow_implicit_invocation: true
